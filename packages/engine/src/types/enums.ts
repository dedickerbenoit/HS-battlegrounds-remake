export enum MinionType {
  Beast,
  Murloc,
  Demon,
  Mech,
  Elemental,
  Pirate,
  Dragon,
  Naga,
  Quilboar,
  All,
}

export enum Tier {
  One = 1,
  Two = 2,
  Three = 3,
  Four = 4,
  Five = 5,
  Six = 6,
  Seven = 7,
}

export enum CardType {
  Minion,
  Spell,
}

export enum GamePhase {
  Init,
  HeroSelection,
  Recruit,
  Combat,
  End,
}

export enum CardLocation {
  Pool,
  Shop,
  Hand,
  Board,
  Graveyard,
  Discovered,
}
